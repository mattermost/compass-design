import { Chip } from '@mattermost/compass-ui/components/chip';
import AccountOutlineIcon from '@mattermost/compass-icons/components/account-outline';
import GlobeIcon from '@mattermost/compass-icons/components/globe';
import avatarEmmaNovak from '@/assets/avatars/Emma Novak.png';
import avatarArjunPatel from '@/assets/avatars/Arjun Patel.png';
import avatarSofiaBauer from '@/assets/avatars/Sofia Bauer.png';
import { RemovableChipsDemo } from './chip.specimen';

Chips are compact, self-contained tokens that represent a selection — a
person added to an invite, a filter applied to a search, a chosen value in
a tokenizing input. Each Chip stands on its own and is removable or
selectable, depending on context.

Use a Chip when a piece of structured data needs to live inline alongside
other values. Reach for a [Tag](/components/tag) instead
when you're labeling content (Bot, Guest, severity), not selecting it.

<Divider />

## Anatomy

- <Num>1</Num> **Container** — pill-shaped surface with a subtle background.
- <Num>2</Num> **Supporting visual** — optional. An avatar for people, an
  icon for filters or properties.
- <Num>3</Num> **Label** — required text describing the value.
- <Num>4</Num> **Remove button** — the control that removes an input chip.

<AnatomyStage style={{gap:'0.5rem',flexWrap:'wrap',alignItems:'center'}}>
  <Chip leadingAvatar={{ src: avatarEmmaNovak, alt: 'Emma Novak' }} onRemove={() => {}}>Emma Novak</Chip>
  <Chip leadingIcon={<GlobeIcon />} onRemove={() => {}}>design</Chip>
  <Chip leadingIcon={<AccountOutlineIcon />}>From: Mira</Chip>
</AnatomyStage>

<Divider />

## Sizes

Sizes line up with the Text Input scale so chips drop into inputs without
adjusting padding.

| Size           | Use when                                                  |
| -------------- | --------------------------------------------------------- |
| Small          | Dense filter rows, table cells.                           |
| Medium         | Default. Composer fields, search filters.                 |
| Medium Compact | Inside compact inputs and tight toolbars.                 |
| Large          | Touch-first inputs and accessibility-led surfaces.        |

<div style={{display:'flex',gap:'0.5rem',flexWrap:'wrap',alignItems:'center'}}>
  <Chip size="small">Small</Chip>
  <Chip size="medium">Medium</Chip>
  <Chip size="medium-compact">Medium Compact</Chip>
  <Chip size="large">Large</Chip>
</div>

<Divider />

## Variants

### Input chips

Input chips appear inside a tokenizing input — adding members to a channel,
filtering a list, picking tags. Each chip carries a remove (×) control.

<div style={{display:'flex',gap:'0.5rem',flexWrap:'wrap',alignItems:'center'}}>
  <Chip leadingAvatar={{ src: avatarEmmaNovak, alt: 'Emma Novak' }} onRemove={() => {}}>Emma Novak</Chip>
  <Chip leadingAvatar={{ src: avatarArjunPatel, alt: 'Arjun Patel' }} onRemove={() => {}}>Arjun Patel</Chip>
  <Chip leadingAvatar={{ src: avatarSofiaBauer, alt: 'Sofia Bauer' }} onRemove={() => {}}>Sofia Bauer</Chip>
</div>

### Choice chips

Choice chips are selectable filters. They toggle on and off but don't get
removed — keep the remove control off.

<div style={{display:'flex',gap:'0.5rem',flexWrap:'wrap',alignItems:'center'}}>
  <Chip leadingIcon={<GlobeIcon />}>All channels</Chip>
  <Chip leadingIcon={<AccountOutlineIcon />} colored>Direct messages</Chip>
</div>

<Divider />

## Error state

Use the error variant when an input chip fails validation — for example, an
email that didn't resolve to a member. Pair it with field-level error text
so the cause is explicit.

<div style={{display:'flex',gap:'0.5rem'}}>
  <Chip error onRemove={() => {}}>{'not-a-member@example.com'}</Chip>
</div>

<Divider />

## Accessibility

A set of chips is one widget. Wrap chips that belong together in a
`ChipGroup`. Tab lands on the active chip; arrows move inside the set;
the × is mouse-only.

- **Tab.** Lands on the active chip. The next Tab leaves the set (to the
  parent input, if there is one).
- **Left / Right.** Move from chip to chip. The × is not a keyboard stop.
- **Delete / Backspace / Enter / Space.** Remove the focused chip.
- **Label.** Each chip is named `{label}. Press Delete to remove.`
- **Focus.** The ring sits on the chip edge (no offset), so it does not
  collide with a surrounding input. The × never takes a ring.
- **Mouse.** Click the × to remove, same as Delete.

<div style={{display:'flex',gap:'0.5rem',flexWrap:'wrap',alignItems:'center'}}>
  <RemovableChipsDemo />
</div>

<Divider />

## Implementation

For props, variants, and copy-ready examples, see the
[Chip specimen page](/components/chip/specimen).
